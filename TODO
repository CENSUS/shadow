---[ New TODO list (Android-specific)

* Test with Google's gdbclient binary.

* Test with Android Studio.
    + Port to lldb.

* Support for huge allocations.

---[ Old TODO list (mostly Firefox-specific)

* lldb_{driver,engine}.py for macOS and lldb.

* Search the jemalloc heap for specific Firefox structures/objects.

* Support for the tenured GC heap.

* Data type discovery of memory contents.

